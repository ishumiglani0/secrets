<%- include('partials/header') %>
<%- include('partials/nav') %>


<div class="jumbotron centered mb-5">
  <div class="container">
    <i class="fas fa-key fa-6x c-w"></i>
    <h1 class="display-3 c-w">Secrets</h1>
    <p class="lead c-w">Don't keep your secrets, share them anonymously!</p>
    <hr class="c-w bg-white">
    <a class="btn btn-light btn-lg" href="/register" role="button">Register</a>
    <a class="btn btn-dark btn-lg" href="/login" role="button">Login</a>

  </div>
</div>

<%- include('partials/below-footer') %>


  <script>
     function below() {

  const elem = document.getElementById('elow')
  const screenWidth = window.outerWidth;

  console.log('ishu' + screenWidth)
  if (screenWidth < 390) {
      elem.classList.add("ishu-b");
      
    } else {
      elem.classList.remove("ishu-b");
  }
}
// Call the function initially and add a resize event listener to handle changes when the window is resized
below();
window.addEventListener("resize", below);
  </script>
<%- include('partials/footer') %>
